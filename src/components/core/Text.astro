---
const { id, variantFullComponent = "default", variantText = "default", text } = Astro.props;
---

<div class={`${variantFullComponent}`}>
  <p id={id} class={`${variantText}`} data-i18n={`${text}`}>{`${text}`}</p>
</div>
